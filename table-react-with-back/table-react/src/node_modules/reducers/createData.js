import { ADD_USER, DATA_USER } from './constants/NewEvent.js';

export default (state = {arr:[]}, action) => {
    switch (action.type) {
    case DATA_USER: 
        return {...state , arr:action.payload};
    
    case ADD_USER:
        return action.data.user;
    
    default:
        return state;
    }
};